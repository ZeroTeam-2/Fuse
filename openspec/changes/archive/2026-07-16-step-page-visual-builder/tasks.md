## 1. Модель данных (shared)

- [x] 1.1 В `packages/shared/src/types/index.ts` ввести новую модель `StepPage` (`title`, `rows: PageRow[]`), типы `PageRow`, `PageBlock`, `PageBlockType` (`input`/`select`/`dropzone`/`richtext`/`paragraph`), `span: 1|2|3|4`, `binding?`; удалить/переопределить старый union и `PageField`.
- [x] 1.2 Ввести хелпер категории блока (тип → `input` | `display`), переиспользуемый фронтом и бэком.
- [x] 1.3 Расширить payload события `page:required` в `packages/shared/src/events/index.ts` картой `resolved: Record<string, unknown>` (значения блоков отображения по `blockId`).
- [x] 1.4 Собрать `@fuse/shared`; убедиться, что зависимые пакеты компилируются против новых типов (ожидаемо появятся ошибки в местах старой модели — они чинятся дальше).

## 2. Конвертер и миграция

- [x] 2.1 Написать конвертер `StepPage(old) → StepPage(new)`: `fields`→строки блоков `input` (`target`→`binding`, `hint`→ведущий `paragraph`), `file`→`dropzone`, `text`→`paragraph`; заголовок сохраняется.
- [x] 2.2 Применить конвертер к сид-данным (`scripts`/seed) и обновить фикстуры со старой моделью.
- [x] 2.3 Покрыть конвертер тестом: старые страницы всех трёх типов дают валидную новую модель без потери заголовка/полей/текста.

## 3. Валидация (backend)

- [x] 3.1 Переписать `apps/backend/src/scenarios/page-validator.ts` под новую модель: непустой `title`, непустые строки, `span` 1–4, допустимые типы блоков, привязки Ввода/Отображения синтаксически корректны (`s{idx}:{key}` для отображения).
- [x] 3.2 Обновить `apps/backend/test/page-validation.spec.ts` под новые правила (валидные/битые раскладки, битые привязки → ошибка/предупреждение по решению в design).

## 4. Ручные входы (backend)

- [x] 4.1 В `apps/backend/src/execution/manual-inputs.ts` переписать `pageFields`/`pageCovers`/`mapPageDataToLocalKeys` на блоки ввода: покрытие по `binding`, ключ данных страницы — `block.id`, значение кладётся под `binding`.
- [x] 4.2 Сохранить правило `source: "page"` — значения, покрытые страницей, не идут в общую форму запуска.
- [x] 4.3 Обновить `apps/backend/test/manual-inputs.spec.ts` и `apps/backend/test/worker-manual-inputs.spec.ts`.

## 5. Разрешение данных отображения (backend)

- [x] 5.1 В `apps/backend/src/execution/worker.service.ts::executeStep` перед `page:required` собрать карту `{ blockId → value }` для блоков отображения: по `binding = s{idx}:{key}` прочитать результат шага-источника из контекста запуска, учитывая массивные выходы теми же правилами, что резолвер маппингов.
- [x] 5.2 Положить карту в `payload.resolved` события `page:required`; недоступный источник → блок пустой, шаг не падает.
- [x] 5.3 Тест воркера: страница с блоком отображения на выход шага 1 отдаёт разрешённое значение в payload.

## 6. Редактор страницы (frontend)

- [x] 6.1 Переписать `apps/frontend/components/scenario/PageEditor.vue` в полноэкранный оверлей по образцу `FormBuilder.jsx`: шапка (назад/заголовок/счётчик/Отмена/Готово), реактивная копия `rows`, эмит новой `StepPage` при сохранении.
- [x] 6.2 Палитра слева: две категории («Ввод»: `input`/`select`/`dropzone`/`richtext`; «Отображение»: `paragraph`) с drag-start.
- [x] 6.3 Лист-канвас: сетка `grid-cols-4`, зоны между строками (`GapZone`), вставка в строку по позиции курсора, направляющие колонок при перетаскивании, автоудаление пустых строк.
- [x] 6.4 Размещённый блок: перетаскивание для переупорядочивания, ручка ресайза со снапом 1–4 (pointer-capture), удаление, inline-редактирование текста `paragraph`.
- [x] 6.5 Инспектор справа: подпись/placeholder/текст по типу, ширина 1–4, привязка, удаление; правки отражаются немедленно.
- [x] 6.6 Привязка Ввода: список ручных значений текущего шага (параметры + `filter:*`); пустой список → подсказка «привязывать не к чему».
- [x] 6.7 Привязка Отображения: список выходов только предыдущих шагов, у которых есть выходы, и их полей (по образцу `prevStepOptions`/`fieldsForStep` из `StepConfig.vue`).

## 7. Интеграция редактора (frontend)

- [x] 7.1 В `apps/frontend/pages/my/scenarios/[id]/edit.vue` передать редактору все `steps` и `schemas` (не только текущий шаг) — для привязок Ввода и Отображения.
- [x] 7.2 В `apps/frontend/components/scenario/StepConfig.vue` секцию «Страница шага» перевести на счётчик элементов (сумма `items`) вместо «типа страницы»; кнопка «Открыть редактор» эмитит `edit-page`.

## 8. Рантайм-рендер (frontend)

- [x] 8.1 Вынести обобщённый рендер раскладки страницы в переиспользуемый компонент (строки/сетка/блоки), заменив три ветки по типам.
- [x] 8.2 Блоки ввода мапятся на `Field`/`Input`, собирают значения (обязательные блокируют), возвращаются через `submitPage` под ключами блоков.
- [x] 8.3 Блоки отображения выводят значение из `payload.resolved` (или пусто).
- [x] 8.4 Подключить рендер в `apps/frontend/components/RunPanel.vue` и `apps/frontend/components/PlaygroundPanel.vue`; проверить восстановление ожидающей страницы (`restoreWaitingPage`).

## 9. Проверка end-to-end

- [x] 9.1 `pnpm` typecheck/линт по фронту, бэку и shared зелёные.
- [x] 9.2 Юнит/интеграционные тесты бэка зелёные (`page-validation`, `manual-inputs`, `worker-manual-inputs`, конвертер).
- [x] 9.3 Ручной прогон: собрать страницу с блоком ввода (привязка к параметру) и блоком отображения (привязка к выходу шага 1); запустить сценарий; убедиться, что ввод уходит в шаг, а отображение показывает данные шага 1.
- [x] 9.4 Прогнать конвертированный засеянный сценарий: старая страница поднимается в новой модели, запуск проходит без регрессий.

## 10. Динамические варианты select из данных шага

- [x] 10.1 Модель: добавить `PageBlock.optionsSource` (`s{idx}:{outKey}`) — источник вариантов `select` из выхода пройденного шага; `optionsSource` перекрывает статические `options`.
- [x] 10.2 Backend: расширить резолвер данных страницы (`resolveDisplayBindings` → `resolvePageBindings`) — разворот массивного поля/результата шага-источника в список строк-вариантов по `blockId` в `payload.resolved`; покрыты обе формы массива (объект с массивным полем и массив объектов).
- [x] 10.3 Редактор: в инспекторе `select` переключатель «Вручную / Из шага»; в динамическом режиме — выбор поля из выходов предыдущих шагов (техническим ключом).
- [x] 10.4 Рантайм: `PageRunner` берёт варианты `select` из `resolved` (массив), иначе — из статических `options`.
- [x] 10.5 Тест воркера: `select` с `optionsSource` на массивное поле шага 1 отдаёт развёрнутый список вариантов в payload.
