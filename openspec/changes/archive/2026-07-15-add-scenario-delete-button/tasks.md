## 1. UI: кнопка удаления и модальное подтверждение

- [x] 1.1 В `apps/frontend/pages/my/scenarios/[id]/edit.vue` добавить в шапку редактора (строка с `PublishButton`) danger-кнопку «Удалить» с иконкой `trash-2` (слот `#left`), повторяющую разметку кнопки из `apps/frontend/pages/my/apps/[id]/index.vue`
- [x] 1.2 Добавить модальное окно подтверждения `Modal` (`v-if="confirmDelete"`) с заголовком «Удалить сценарий?», подзаголовком с названием сценария, текстом-предупреждением и футером «Отмена» / «Удалить» (оба `Button`, удалить — `variant="danger"`)
- [x] 1.3 Добавить реактивный флаг `confirmDelete = ref(false)`; кнопка «Удалить» открывает модалку, «Отмена» и `@close` — закрывают

## 2. Логика удаления

- [x] 2.1 Реализовать `deleteScenario()`: вызывать `$api.DELETE("/api/scenarios/{id}", { params: { path: { id: scenarioId } } })`, на успехе — `navigateTo("/my/scenarios")`, на ошибке — `confirmDelete.value = false` (повторяет `deleteApp`)
- [x] 2.2 Привязать `deleteScenario` к кнопке «Удалить» в футере модального окна

## 3. Проверка

- [x] 3.1 Запустить фронтенд (lint/typecheck по проекту) и убедиться, что ошибок нет
- [x] 3.2 Вручную проверить: кнопка видна → модалка открывается → отмена не удаляет → подтверждение удаляет и редиректит на `/my/scenarios`
