# Tasks — add-page-step-type

## 1. Shared: модель и схема шага-страницы

- [x] 1.1 Добавить `PAGE: "page"` в `StepType` и интерфейс `PageStep extends BaseStep { type: "page"; page: StepPage }` в union `Step` (`packages/shared/src/enums`, `src/types`)
- [x] 1.2 Удалить `BaseStep.page` из модели — страницу несёт только `PageStep`
- [x] 1.3 Добавить в shared хелпер `pageStepSchema(page: StepPage): StepSchema` — выходы из блоков ввода (`key = binding ?? id`, `label = block.label ?? key`, `dropzone → "file"`, остальные → `"string"`), `inputs: []`, `outputIsArray: false`; покрыть unit-тестом
- [x] 1.4 Добавить хелпер получения ключа выхода блока и проверку уникальности ключей страницы (для валидации на бэке и в инспекторе)

## 2. Backend: исполнение page-шага

- [x] 2.1 В `worker.service.ts` добавить ветку `case "page"`: при наличии блоков ввода — `requestUserInput` с `page:required` (payload: page + `resolvePageBindings`), маппинг данных сабмита `blockId → binding ?? id`, проверка непустоты обязательных ключей, запись собранных значений результатом шага
- [x] 2.2 Display-only страница (без блоков ввода): публикация `page:required` с резолвнутыми данными, результат шага `{}`, продолжение без ожидания сабмита
- [x] 2.3 Удалить легаси-ветку `if (step.page)` из `executeStep` вместе со старой семантикой `mapPageDataToLocalKeys` (новый маппинг «blockId → ключ выхода» живёт в ветке page-шага)
- [x] 2.4 Проверить резолв `s{idx}:{key}` из результата page-шага, включая `UploadedFileRef` целиком (multipart api-шага срабатывает от файла из результата страницы)

## 3. Backend: схема, ручной ввод, валидация

- [x] 3.1 `ManualInputsService.loadStepSchema` и `ScenariosService.getStepSchema`: для `page`-шага возвращать `pageStepSchema` (в т.ч. когда page-шаг — последний шаг вложенного сценария)
- [x] 3.2 `manual-inputs.ts`: шаги типа `page` не порождают дескрипторов формы запуска; удалить `pageCovers` и признак `source: "page"` у `ManualInputDescriptor` (все дескрипторы спрашивает форма) вместе с потребителями на фронте (бэкенд готов; фронт-потребители — в группе 5)
- [x] 3.3 Валидация сценария при сохранении: `page`-шаг обязан нести непустую страницу (≥1 блок), ключи выходов уникальны
- [x] 3.4 Проверить смежные места, где шаг фильтруется по `appId` (`cycle-guard`, вкладка «Окружения», подсчёт провайдеров в `marketplace.service.ts`): page-шаг не участвует, как `delay`

## 4. Frontend: редактор

- [x] 4.1 `AddStepMenu`/`StepPicker`: тип «Страница» (без выбора приложения/endpoint)
- [x] 4.2 `StepConfig.vue`: панель page-шага — название, «Открыть редактор», сводка «N элементов»; полностью удалить секцию «Страница» у остальных типов
- [x] 4.3 `PageEditor.vue`, инспектор блока ввода: поле «Ключ выхода» (валидация формата и уникальности, пусто → id блока) вместо селекта привязки к параметрам шага; инспектор блока отображения — источники только из шагов до шага-страницы
- [x] 4.4 Выходы page-шага в источниках маппинга «Из шага» — через `pageStepSchema` на сервере (схемы шагов грузятся из API после каждого сохранения); сайд-эффект «привязка блока → `mappings[key] = "user"`» удалён (`reconcileSources` в edit.vue); добавление page-шага сразу открывает конструктор, persist — по «Готово» (сервер не принимает пустую страницу)
- [x] 4.5 Сводка page-шага в списке шагов: показывать выходы (ключи блоков ввода)

## 5. Frontend: запуск

- [x] 5.1 `RunPanel.vue`/`PageRunner.vue`: рендер `page:required` от page-шага (сабмит как сейчас); display-only страница рендерится без формы сабмита
- [x] 5.2 Финальный экран: если display-only страница — последний шаг, `run:done` не скрывает её; сводка результата доступна рядом
- [x] 5.3 Форма запуска: убедиться, что значения page-шагов не спрашиваются (дескрипторов нет) и обязательные блоки валидируются на странице (фильтр `source` удалён в RunPanel и PlaygroundPanel)

## 6. Тесты и проверка

- [x] 6.1 Unit-тесты worker: page-шаг с вводом (пауза, результат = значения), display-only (без паузы, финальная страница) + пустое обязательное значение = доменная ошибка
- [x] 6.2 Unit-тесты manual-inputs: page-шаг не порождает дескрипторов, значения страницы не спрашиваются формой
- [x] 6.3 Обновить `seed-local.mts` (демо-сценарий «Демо: страницы»: ввод → эхо → финальная display-страница) и сбросить локальную базу (`pnpm infra:reset` + `pnpm seed`)
- [x] 6.4 e2e `page-step-run.spec.ts`: запуск сид-сценария — страница ввода блокирует пустое обязательное поле, значение доезжает до api-шага, финальная страница остаётся после `run:done` (создание в редакторе покрыто существующим scenario-create + юнитами конструктора)
- [x] 6.5 Прогнать headless-проверку запуска по памятке `fuse-run-verification`: page-шаг ждёт ввода, результат = `{ inn }` по ключу выхода, api-шаг получил значение через `s0:inn`, display-only финал не блокировал completed
