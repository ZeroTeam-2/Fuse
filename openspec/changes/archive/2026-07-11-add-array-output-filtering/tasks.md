## 1. Модель данных (`packages/shared`)

- [x] 1.1 Добавить в `SchemaField` (`packages/shared/src/types/index.ts`) опциональное `items?: SchemaField[]` — поля элемента для `type: "array"`
- [x] 1.2 Добавить типы `FilterOperator` (`eq` | `ne` | `gt` | `lt` | `gte` | `lte` | `contains`), `StepFilterValue` (`mode: "user" | "const" | "ref"`, `const?`, `ref?`), `StepFilter` (`arrayPath?`, `field`, `op`, `value`)
- [x] 1.3 Добавить в `BaseStep` опциональное `filters?: Record<string, StepFilter>` (ключ = key входного параметра шага)
- [x] 1.4 Добавить тип ответа схемы шага с признаком массива (`{ inputs, outputs, outputIsArray }`) и экспортировать всё новое из `packages/shared/src/index.ts`

## 2. Импорт OpenAPI: перестать терять массив

- [x] 2.1 В `apps/backend/src/apps/openapi-parser.ts` разделить извлечение выходов: `extractOutputs` возвращает `{ fields, isArray }` — при `schema.type === "array"` поля берутся из `items`, но факт массива сохраняется, а не отбрасывается
- [x] 2.2 Там же: для свойств типа `array` заполнять `SchemaField.items` полями элемента (глубина 1, как остальной парсер)
- [x] 2.3 В `apps/backend/src/apps/app.schema.ts` добавить `items` в `SchemaFieldDoc` (сабсхема strict — без этого поле молча не сохранится) и `outputIsArray` в схему endpoint'а
- [x] 2.4 Прокинуть признак через сервис приложений при импорте и переимпорте спецификации — правок не потребовалось: `create` кладёт `parsed.endpoints` целиком, `applyReimport` спредит `...ep`
- [x] 2.5 Тест (`apps/backend/test/openapi-parser.spec.ts`, новый): ответ-массив объектов → выходы = поля элемента + `outputIsArray: true`; объект с полем-списком → у поля есть `items`. **Круговой путь через Mongoose-схему в vitest невозможен** (нет `emitDecoratorMetadata` → `@Prop` не выводит типы, импорт `app.schema.ts` падает); персистентность проверяется задачей 7.2 на реальной БД

## 3. Схема шага отдаёт признак массива

- [x] 3.1 В `apps/backend/src/scenarios/scenarios.service.ts` расширить `getStepSchema` (и `getApiStepSchema` / `getScenarioRefStepSchema`) — возвращать `outputIsArray` из endpoint'а; для вложенного сценария признак берётся у его последнего шага
- [x] 3.2 Типизация ответа `GET /api/scenarios/:id/step-schema/:index` — общий тип `StepSchema` из `@fuse/shared` на обеих сторонах. Перегенерация `apps/frontend/src/types/api.ts` не нужна: контроллер не объявляет response-DTO, у эндпоинта в схеме нет типа, фронт и так читает ответ через каст

## 4. Резолвер: отбор элемента массива

- [x] 4.1 В `apps/backend/src/execution/mapping-resolver.ts` добавить чистую функцию сравнения `matchesFilter(item, filter, operandValue)` — приведение операнда по фактическому типу значения элемента (number → числовое сравнение, boolean → `true`/`false`, иначе строка; `contains` = регистронезависимая подстрока)
- [x] 4.2 Добавить резолв операнда фильтра: `const` (через `resolveTemplate` + `buildTemplateContext`), `ref` (`s{idx}:{key}`, при массиве — первый элемент), `user` (ключ `filter:{paramKey}` в `userInput`). Попутно: regex шаблона расширен до `[\w:]+` — `\w` не матчит двоеточие, поэтому `{{s0:key}}` НЕ подставлялся вообще
- [x] 4.3 Переписать ветку `s{idx}:{key}` в `resolveMappings`: найти массив (`filter.arrayPath` → поле результата, иначе сам результат); не массив → текущее поведение; массив без фильтра → первый элемент; массив с фильтром → отбор
- [x] 4.4 Реализовать поведение выборки: 0 совпадений → бросить доменную ошибку с текстом (шаг, поле, условие); >1 → первый элемент + предупреждение; заданный `arrayPath` указывает не на массив → предупреждение. `StepExecutionError` вынесён из `worker.service.ts` в `execution-errors.ts` — резолвер бросает именно его, поэтому сбой считается детерминированным и не ретраится
- [x] 4.5 Канал предупреждений — типизированное поле `RunStepResult.warnings?: string[]` (не примесь в `result`, чтобы не ломать отрисовку payload'а в простом режиме и playground)
- [x] 4.6 Тесты в `apps/backend/test/mapping-resolver.spec.ts`: массив + фильтр по каждому оператору; числовое сравнение (`amount > 1000` не сравнивает строки); 0 совпадений → ошибка; >1 → первый + предупреждение; массив без фильтра → первый элемент; `arrayPath` на вложенное поле-список; операнд из `const`-шаблона и из `ref`

## 5. Worker: пользовательский ввод доходит до резолвера

- [x] 5.1 В `apps/backend/src/execution/worker.service.ts` перестать отбрасывать результат `waitForPageInput` — сложить данные страницы шага в `StepContext`
- [x] 5.2 Передавать `userInput` третьим аргументом в `resolveMappings` во всех местах вызова (`executeApiStep` и `executePeriodicStep`); вложенный сценарий наследует `userInput` и сборник предупреждений
- [x] 5.3 Обработать доменную ошибку фильтра: резолвер бросает `StepExecutionError` → существующая ветка `executeRun` фиксирует `failed` и подтверждает сообщение. Попутно: `warnings` добавлены в strict-сабсхему `RunStepResultDoc` (`run.schema.ts`), иначе поле молча не сохранилось бы

## 6. Фронтенд: блок фильтра в настройке шага

- [x] 6.1 В `apps/frontend/components/scenario/StepConfig.vue` завести состояние фильтра рядом с `FieldState` и гидрировать его из `step.filters` (не сломать текущий `hydrateKey`-watch — он уже стережёт затирание незавершённого выбора)
- [x] 6.2 Показывать блок фильтра только когда выбранный шаг-источник отдаёт массив (`outputIsArray` из схемы) либо выбранное поле результата приехало из поля-списка. **Уточнение модели:** «Поле результата» всегда называет поле, которое реально подставится в параметр, поэтому поле-список разворачивается в поля его элемента прямо в этом селекте (значение опции кодирует список и поле: `{arrayPath}::{key}`). Выбирать сам массив бессмысленно — из него нечего подставить в параметр
- [x] 6.3 Собрать UI условия на существующих DS-компонентах: `Select` (поле элемента) + `Select` (оператор) + `SegmentedControl` (ручной ввод / константа / из шага) + `Input` или пара `Select`ов для значения
- [x] 6.4 Ограничить набор операторов типом выбранного поля элемента: `boolean` → только `=` / `≠`; `contains` → только `string`
- [x] 6.5 Сериализовать фильтр в `step.filters[paramKey]` в `emitUpdate` — по аналогии с `mappings`/`consts`, включая правило «не писать наполовину заданное условие» (иначе повторится баг с прыгающей вкладкой)
- [x] 6.6 В `apps/frontend/pages/my/scenarios/[id]/edit.vue` показывать фильтр в сводке параметра (`stepParams`), напр. «Шаг 1 · id, где inn = 7707083893»

## 7. Проверка

- [x] 7.1 `pnpm test` (128 тестов, 13 файлов) и `pnpm --filter @fuse/frontend typecheck` — зелёные; `oxlint` по изменённым областям чист
- [x] 7.2 Круговой путь проверен на **реальной БД** скриптом на скомпилированном коде (`dist`): парсер → strict-сабсхема → запись → чтение свежим запросом. `outputIsArray=true` у коллекции и `items[].keys=[id,inn,amount]` у поля-списка сохраняются
- [x] 7.3 В редакторе собран сценарий «шаг 1 отдаёт коллекцию → шаг 2 берёт `id` по условию `inn = 7707083893`»; сохранён `filters: {orgId: {field: "inn", op: "eq", value: {mode: "const", const: "7707083893"}}}`; после перезагрузки страницы восстановлены и режим, и все селекты, и значение. Сводка в списке шагов: «Шаг 1 · id, где inn = 7707083893». Ограничение операторов проверено на числовом поле («содержит» исчезает)
- [x] 7.4 Живой прогон через воркер и очередь: сценарий «список организаций → карточка организации по условию `name содержит ОАО «Отзыв»`» завершается со статусом `completed` — в путь уезжает `id` отобранной организации, API отвечает 200. Проверено дважды: созданием `Run` + сообщения в реальную очередь (мной) и запуском из UI (владельцем). Пустая и неоднозначная выборка покрыты юнит-тестами в `mapping-resolver.spec.ts` — вживую не воспроизводились

## 8. Блокеры, вскрытые при живой проверке (не были запланированы)

- [x] 8.1 **Переимпорт спеки не работал вообще.** `applyReimport()` в `apps.service.ts` не был подключён ни к одному маршруту; кнопка «Сохранить обновление» звала `PATCH /api/apps/:id` (тупой `$set` метаданных) — `endpoints`/`specSnapshot`/`syncedAt` не обновлялись НИКОГДА, а UI рапортовал об успехе, потому что `openapi-fetch` не бросает, а `saveUpdate()` ловил `try/catch`. Добавлен `POST /api/apps/:id/reimport/apply`, кнопка переведена на него, ошибка показывается честно. Без этого фичу нельзя было даже включить: у старых приложений не появлялся `outputIsArray`
- [x] 8.2 **Path/query/header-параметры не подставлялись никогда.** `applyPathParams`/`appendQuery` читали `resolved.path`/`resolved.query`, а `resolveMappings` отдаёт плоскую карту по ключам полей — в URL уезжал литеральный `{guid}`, и API отвечал 422. Добавлена чистая функция `groupInputsByLocation(resolved, inputs, pathTemplate)`: место берётся из `SchemaField.loc` эндпоинта, плейсхолдер пути важнее схемы, неизвестные поля → тело; значение path-параметра URL-экранируется. Покрыто тестами + проверено на реальных данных (реальный вызов API вернул 200). Требование добавлено в дельту `scenario-execution`

## 9. Известное ограничение (в этот change НЕ входит)

- [ ] 9.1 Значение условия в режиме «Ручной ввод» у шага **без страницы ввода** спросить негде: форма запуска собирает только входы первого шага и не знает про ключи `filter:{paramKey}`. Значение придёт пустым → шаг упадёт с «условие не отобрало ни одного элемента». Выносится в отдельный change (сбор всех `user`-значений по всем шагам)
