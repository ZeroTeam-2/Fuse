## 1. Общие типы и контракты (packages/shared)

- [x] 1.1 Добавить `ManualInputDescriptor` (`key`, `stepPath`, `stepIndex`, `stepTitle`, `paramKey`, `kind: "param" | "filter"`, `label`, `type`, `required`, `source: "form" | "page"`) в `src/types/index.ts`
- [x] 1.2 Расширить `CreateRunDto` до `{ scenarioId: string; inputs?: Record<string, unknown> }`
- [x] 1.3 Добавить `PageField.target?: string` — локальный ключ значения шага, которое заполняет поле страницы (`inn` или `filter:status`)
- [x] 1.4 Добавить в `src/events/index.ts` серверное событие `input:required` с payload `{ stepIndex, stepTitle, fields: ManualInputDescriptor[] }` и включить его в `ServerWsEvent`
- [x] 1.5 Проверить, что пакет собирается и типы экспортируются наружу (`pnpm --filter shared build` или эквивалент)

## 2. Перечисление ручных значений (backend)

- [x] 2.1 Создать `apps/backend/src/execution/manual-inputs.ts`: чистые функции построения скоуп-ключа (`s{i}[.s{j}]:{paramKey}` и `…:filter:{paramKey}`), разбора ключа и среза входов по префиксу пути шага
- [x] 2.2 Реализовать обход шагов сценария: собрать дескрипторы для `mappings[key] === "user"` (kind `param`) и для `filters[key].value.mode === "user"` (kind `filter`, `required: true` всегда)
- [x] 2.3 Дорезать подписи и типы: для `param` — из `inputs` схемы шага (`ScenariosService.getStepSchema`); для `filter` — из схемы элемента массива шага-источника (источник из `mappings[key] = "s{idx}:{outKey}"`, поле из `filter.field`, тип из `items`), с деградацией в `string` при бедной схеме
- [x] 2.4 Проставить `source`: `"page"`, если у шага есть страница типа «Ввод полей» с полем, привязанным к этому значению (`field.target === localKey`), либо — при отсутствии `target` — с полем, чей `key` совпадает с `paramKey`; иначе `"form"`
- [x] 2.5 Рекурсивно спускаться в шаги типа `scenario` с префиксом пути, множеством посещённых `refScenarioId` вдоль пути и пределом глубины; замыкающую цикл ссылку пропускать
- [x] 2.6 Сломанный шаг (нет приложения/endpoint'а) пропускать в перечислении, не роняя запрос
- [x] 2.7 Юнит-тесты перечислителя: параметры и фильтры по разным шагам; одноимённые ключи на разных шагах не склеиваются; вложенный сценарий; цикл A→B→A завершается; значение, к которому привязано поле страницы, помечено `source: "page"`; непривязанное — `"form"`; старое правило совпадения ключей (поле без `target`) по-прежнему даёт `"page"`

## 3. API перечисления (backend)

- [x] 3.1 Добавить `GET /api/scenarios/:id/manual-inputs`, возвращающий `ManualInputDescriptor[]` (рядом с существующим `step-schema/:index`, та же авторизация)
- [x] 3.2 Публичный `GET /api/marketplace/:id/manual-inputs` для формы запуска: панель запуска видна гостю, а `/api/scenarios/*` под JWT
- [x] 3.3 e2e/контроллерный тест: сценарий с ручными значениями отдаёт непустой список; сценарий без них — пустой

## 4. Модель запуска и приём входов (backend)

- [x] 4.1 `run.schema.ts`: добавить `inputs?: Record<string, unknown>` (`Mixed`); заменить `pageData` на `pendingInput?: { stepIndex: number; data: Record<string, unknown> }`
- [x] 4.2 `POST /api/runs` принимает `inputs`, сохраняет их в `Run.inputs`
- [x] 4.3 `POST /api/runs` валидирует полноту: перечислить ручные значения сценария и вернуть `400` со списком недостающих обязательных ключей
- [x] 4.4 Добавить `POST /api/runs/:id/input-submit` с `{ stepIndex, values }`: писать `pendingInput`, мержить `values` в `Run.inputs` по скоуп-ключам, переводить статус в `running` (проверки те же, что у `page-submit`: статус `waiting_input` и совпадение `currentStep`)
- [x] 4.5 Перевести `page-submit` на `pendingInput`
- [x] 4.6 Тесты сервиса: создание запуска с входами; 400 при недостающем обязательном; `input-submit` дописывает значения в `Run.inputs`

## 5. Резолвер маппингов (backend)

- [x] 5.1 Убрать фолбэк `userInput?.[field] ?? userInput` — отсутствующее значение остаётся `undefined`
- [x] 5.2 Писать предупреждение в `warnings` при отсутствии `user`-значения (для необязательных — это штатный путь)
- [x] 5.3 Переписать кейс `test/mapping-resolver.spec.ts` «falls back to entire userInput when field is absent» под новое поведение; добавить тест на разрешение операнда фильтра из `filter:{paramKey}`

## 6. Воркер: доставка входов и добор (backend)

- [x] 6.1 Засеивать `userInput` из `Run.inputs` (срез по пути шага) и **не терять его между шагами** — вынести накопленный ввод из тела цикла шагов (`worker.service.ts:249-255`)
- [x] 6.2 Данные страницы шага перед мержем переводить из ключей полей в локальные ключи значений по `field.target` (поле без `target` — по своему `key`, как сейчас) и мержить поверх среза входов: страница перекрывает значение из входов запуска
- [x] 6.3 Передавать во вложенный сценарий срез входов, адресованных его шагам (с учётом префикса пути)
- [x] 6.4 Обобщить `waitForPageInput` в `waitForUserInput(runId, stepIndex, request)`: общий цикл ожидания (`waiting_input`, публикация события, опрос, отмена, таймаут), различается только публикуемое событие и читаемый ящик
- [x] 6.5 Читать `run.pendingInput ?? run.pageData` для совместимости с запусками в полёте на момент деплоя
- [x] 6.6 Перед исполнением шага сверять дескрипторы шага с накопленным `userInput`: при отсутствии **обязательного** значения публиковать `input:required` и ждать; необязательные не запрашивать
- [x] 6.7 Добранные значения дописывать в `Run.inputs`, чтобы не спрашивать их повторно после перезапуска воркера
- [x] 6.8 Таймаут ожидания добора завершать доменной ошибкой с диагностикой (шаг, параметр), `Run` → `failed`
- [x] 6.9 Тесты воркера: значение из `Run.inputs` доезжает до шага без страницы; ввод не теряется между шагами; операнд фильтра из входов запуска отбирает элемент; данные страницы с `target` доезжают до параметра при несовпадающем ключе поля; отсутствие обязательного значения уводит в `waiting_input`, а не в `failed`; повторная обработка запуска не запрашивает уже добранное

## 7. Фронтенд: форма ручных входов

- [x] 7.1 Создать `components/ui/Field.vue` — контрол по типу значения (`number` → числовой ввод, `boolean` → переключатель, остальное → текст), возвращающий типизированное значение и поддерживающий `required`
- [x] 7.2 Создать `components/run/ManualInputsForm.vue` на `ManualInputDescriptor[]`: группировка по шагам (заголовок шага → подписи параметров), для `kind: "filter"` — подпись про значение условия отбора, обязательные поля блокируют submit и подсвечиваются
- [x] 7.3 `RunPanel.vue`: на экране запуска тянуть `GET /api/scenarios/:id/manual-inputs`; при непустом списке показывать форму и слать `inputs` в `POST /api/runs`; при пустом — оставить запуск в один клик
- [x] 7.4 `PlaygroundPanel.vue`: тот же экран сбора входов и та же блокировка запуска (паритет с простым режимом)
- [x] 7.5 `composables/useRunSocket.ts`: подписаться на `input:required`
- [x] 7.6 Обработка `input:required` в `RunPanel` и `PlaygroundPanel`: показать ту же `ManualInputsForm` с пришедшими дескрипторами и отправить значения в `POST /api/runs/:id/input-submit`
- [x] 7.7 Восстановление после перезагрузки страницы: снапшот со статусом `waiting_input`, вызванным добором, снова показывает форму (по аналогии с `restoreWaitingPage`)

## 8. Редактор страниц: привязка поля к значению шага

- [x] 8.1 `PageEditor.vue`: у поля страницы добавить селект «Заполняет» (`field.target`) со списком ручных значений **этого** шага — параметры и операнды условий фильтрации; пункт «не привязано» оставить
- [x] 8.2 Наполнять селект из самого шага (`mappings`/`filters`) и загруженной редактором схемы шага, а не запросом на сервер: редактор держит шаг в памяти, поэтому селект видит и несохранённые правки
- [x] 8.3 Освободить подпись поля: `syncFieldKey` остаётся генератором технического `key`, но подпись больше не обязана совпадать с ключом параметра; read-only `<code>{{ field.key }}</code>` (`:124`) заменить на отображение привязки
- [x] 8.4 Проверить обратную совместимость: страница с полями без `target` сохраняется и работает по прежнему правилу совпадения ключей

## 9. Конструктор: честный источник значения

- [x] 9.1 `StepConfig.vue`: убрать ложную подсказку «Страница не настроена — значения ручного ввода запросятся общей формой» (`:738-740`) и текст про «введёт при запуске», не соответствующий фактическому источнику
- [x] 9.2 Показывать у значения с ручным вводом фактический источник: «Запросит страница шага» (поле страницы привязано к нему) либо «Запросит форма перед запуском» — по `source` из перечисления

## 10. Сторож тишины (потерянный поток событий)

- [x] 10.1 `useRunSocket`: таймер, сбрасываемый каждым событием и подключением; после `SILENCE_MS` без событий — вызов `onSilence`, дальше удвоение интервала до потолка; `stopWatchdog()` наружу
- [x] 10.2 `RunPanel`: по тишине перечитывать `GET /api/runs/:id` и применять через существующий `applySnapshot`; не перечитывать в фазе ожидания ввода (затрёт набранное); останавливать сторож на терминальной фазе
- [x] 10.3 `PlaygroundPanel`: то же — вынести применение состояния запуска в `applyRunSnapshot` (снапшот сокета и перечитывание идут одним кодом)
- [x] 10.4 Зафиксировать в спеке `scenario-execution`: поток событий не единственный источник прогресса; при масштабировании воркеров события обязаны идти через общую шину (socket.io Redis adapter)

## 11. Проверка и документация

- [x] 11.1 Прогнать бэкенд-тесты и линт; убедиться, что тест на старый фолбэк резолвера обновлён, а не удалён
- [x] 11.2 Ручная проверка сквозного пути: сценарий из 3 шагов, где ручной ввод помечен у параметра шага 3 и у значения условия фильтрации шага 2, без страниц ввода → форма собирает оба значения, шаги отрабатывают, ошибки «условие не отобрало ни одного элемента» нет
- [x] 11.3 Ручная проверка гибрида: запуск через API без `inputs` (обязательное значение) → `waiting_input`, форма добора в UI, продолжение исполнения
- [x] 11.4 Ручная проверка привязки: у шага страница с полем «ИНН организации», привязанным к параметру `inn` → значение спрашивается только страницей, в форме перед запуском его нет, шаг вызывается с введённым ИНН
- [x] 11.5 Ручная проверка совместимости: сценарий со страницей, где поле названо ключом параметра и `target` не задан, отрабатывает как раньше
- [ ] 11.6 Обновить `openspec/specs/scenario-execution/spec.md` при архивации (снятие оговорки о том, что сбор `user`-значений у шагов без страницы вне охвата)
