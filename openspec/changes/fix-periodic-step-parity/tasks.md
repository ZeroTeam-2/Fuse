# fix-periodic-step-parity — tasks

## 1. Тип и создание шага

- [x] 1.1 Добавить `endpointId?: string` в `PeriodicStep` (`packages/shared/src/types/index.ts`)
- [x] 1.2 В `StepPicker.vue` записывать `endpointId: ep.id` при создании periodic-шага

## 2. Схема шага на бэкенде

- [x] 2.1 В `ScenariosService` добавить резолв endpoint для periodic-шага: по `endpointId`, фолбэк — совпадение `pollMethod` + `pollPath` среди endpoints приложения; не найден → `EMPTY_SCHEMA` (без 404)
- [x] 2.2 Подключить ветку `case "periodic"` в `getStepSchema` и в `getStepSchemaForStep` (выходы periodic-шага как последнего шага вложенного сценария)
- [x] 2.3 Убедиться, что панель `StepConfig.vue` без правок показывает секции Path/Query/Header/Body и выходы для periodic-шага (новый и легаси-шаг)

## 3. Исполнение в воркере

- [x] 3.1 Вынести общий хелпер сборки запроса (endpoint → `groupInputsByLocation` → URL + `RequestInit` с заголовками/телом) из `executeApiStep`
- [x] 3.2 `executePeriodicStep`: разрешать endpoint (по `endpointId`, фолбэк по `pollMethod`/`pollPath`), собирать URL и опции один раз до цикла, каждый опрос выполнять с полными заголовками/телом/query
- [x] 3.3 Проверить, что легаси-шаг без найденного endpoint исполняется как раньше (плейсхолдеры пути + остальные входы в тело)

## 4. Верификация

- [x] 4.1 Прогнать сценарий с periodic-шагом, у которого есть path-, query- и header-входы (Fake Dadata): в редакторе видны все секции, запуск подставляет параметры на каждой итерации опроса
- [x] 4.2 Проверить, что выходы periodic-шага доступны следующему шагу «Из шага» и подставляются при запуске
- [x] 4.3 Протестировать шаг «Другой сценарий»: вложенный сценарий исполняется как один шаг, его выходы доступны дальше
- [x] 4.4 Протестировать шаг «Задержка»: пауза выдерживается, поток продолжается, тайминг шага записывается в `Run`
- [x] 4.5 Прогнать существующий (легаси) сценарий с periodic-шагом без `endpointId` — поведение не регрессировало
