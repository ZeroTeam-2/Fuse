# Tasks — add-page-step-type

## 1. Shared: модель и схема шага-страницы

- [ ] 1.1 Добавить `PAGE: "page"` в `StepType` и интерфейс `PageStep extends BaseStep { type: "page"; page: StepPage }` в union `Step` (`packages/shared/src/enums`, `src/types`)
- [ ] 1.2 Пометить `BaseStep.page` как `@deprecated` (легаси встроенных страниц) с комментарием о новой семантике
- [ ] 1.3 Добавить в shared хелпер `pageStepSchema(page: StepPage): StepSchema` — выходы из блоков ввода (`key = binding ?? id`, `label = block.label ?? key`, `dropzone → "file"`, остальные → `"string"`), `inputs: []`, `outputIsArray: false`; покрыть unit-тестом
- [ ] 1.4 Добавить хелпер получения ключа выхода блока и проверку уникальности ключей страницы (для валидации на бэке и в инспекторе)

## 2. Backend: исполнение page-шага

- [ ] 2.1 В `worker.service.ts` добавить ветку `case "page"`: при наличии блоков ввода — `requestUserInput` с `page:required` (payload: page + `resolvePageBindings`), маппинг данных сабмита `blockId → binding ?? id`, проверка непустоты обязательных ключей, запись собранных значений результатом шага
- [ ] 2.2 Display-only страница (без блоков ввода): публикация `page:required` с резолвнутыми данными, результат шага `{}`, продолжение без ожидания сабмита
- [ ] 2.3 Сохранить легаси-ветку `if (step.page)` для остальных типов шагов без изменений; убедиться, что page-шаг в неё не попадает
- [ ] 2.4 Проверить резолв `s{idx}:{key}` из результата page-шага, включая `UploadedFileRef` целиком (multipart api-шага срабатывает от файла из результата страницы)

## 3. Backend: схема, ручной ввод, валидация

- [ ] 3.1 `ManualInputsService.loadStepSchema` и `ScenariosService.getStepSchema`: для `page`-шага возвращать `pageStepSchema` (в т.ч. когда page-шаг — последний шаг вложенного сценария)
- [ ] 3.2 `manual-inputs.ts`: шаги типа `page` не порождают дескрипторов формы запуска; `pageCovers`/`source: "page"` остаются только для легаси встроенных страниц
- [ ] 3.3 Валидация сценария при сохранении: `page`-шаг обязан нести непустую страницу (≥1 блок), ключи выходов уникальны
- [ ] 3.4 Проверить смежные места, где шаг фильтруется по `appId` (`cycle-guard`, вкладка «Окружения», подсчёт провайдеров в `marketplace.service.ts`): page-шаг не участвует, как `delay`

## 4. Frontend: редактор

- [ ] 4.1 `AddStepMenu`/`StepPicker`: тип «Страница» (без выбора приложения/endpoint)
- [ ] 4.2 `StepConfig.vue`: панель page-шага — название, «Открыть редактор», сводка «N элементов»; удалить секцию «Страница» у остальных типов; для легаси-шага со встроенной страницей — пометка «встроенная страница (устарело)» и кнопка удаления
- [ ] 4.3 `PageEditor.vue`, инспектор блока ввода: поле «Ключ выхода» (валидация формата и уникальности, пусто → id блока) вместо селекта привязки к параметрам шага; инспектор блока отображения — источники только из шагов до шага-страницы
- [ ] 4.4 Стор `scenario-editor.ts`: модель page-шага (создание/сохранение), выходы page-шага из `pageStepSchema` в источниках маппинга «Из шага» последующих шагов; убрать сайд-эффект «привязка блока → `mappings[key] = "user"`» для page-шагов
- [ ] 4.5 Сводка page-шага в списке шагов: показывать выходы (ключи блоков ввода)

## 5. Frontend: запуск

- [ ] 5.1 `RunPanel.vue`/`PageRunner.vue`: рендер `page:required` от page-шага (сабмит как сейчас); display-only страница рендерится без формы сабмита
- [ ] 5.2 Финальный экран: если display-only страница — последний шаг, `run:done` не скрывает её; сводка результата доступна рядом
- [ ] 5.3 Форма запуска: убедиться, что значения page-шагов не спрашиваются (дескрипторов нет) и обязательные блоки валидируются на странице

## 6. Тесты и проверка

- [ ] 6.1 Unit-тесты worker: page-шаг с вводом (пауза, результат = значения), display-only (без паузы), легаси встроенная страница (прежнее поведение)
- [ ] 6.2 Unit-тесты manual-inputs: page-шаг без дескрипторов, легаси `pageCovers` работает
- [ ] 6.3 Обновить `seed-local.mts`: демо-сценарий с page-шагом ввода и финальной display-страницей
- [ ] 6.4 e2e: сценарий «страница ввода → api-шаг → финальная страница с ответом» (создание в редакторе + запуск)
- [ ] 6.5 Прогнать headless-проверку запуска по памятке `fuse-run-verification` (локальная очередь LocalStack + Mongo 27018)
