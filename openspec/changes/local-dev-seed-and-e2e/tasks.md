## 1. Мок-API в инфраструктуре

- [x] 1.1 Добавить в `docker-compose.dev.yml` сервис-мок (`mccutchen/go-httpbin`) с портом на хосте; включить его в `pnpm infra`
- [x] 1.2 Обновить таблицу портов в README (мок-API — 8085)

## 2. Сид локальной БД

- [x] 2.1 Написать скрипт сида (`apps/backend/scripts/seed-local.mts`): пользователь, демо-приложение с `baseUrl` на мок-API и эндпоинтом-эхо
- [x] 2.2 Добавить два сценария: герметичный `delay` (2 сек) и `api`-шаг к моку
- [x] 2.3 Идемпотентность: upsert по стабильным ключам; три прогона подряд дают `users=1 apps=1 scenarios=2`
- [x] 2.4 Защита от прода: нелокальный хост в `MONGODB_URL` → отказ с кодом 1, ничего не записано (проверено)
- [x] 2.5 Скрипт `seed` в корневом `package.json`; типы шагов берутся из `@fuse/shared`
- [x] 2.6 [сверх плана] Сид пишет `.seed.json` с id сущностей — e2e берёт оттуда пользователя и сценарий; файл в `.gitignore`

## 3. Вход без OAuth

- [x] 3.1 `e2e/auth.setup.ts` подписывает JWT (`{ userId, email }`) через `node:crypto` (HS256, без новой зависимости) и кладёт в куку `access_token`
- [x] 3.2 Setup проверяет, что бэкенд принял куку (`GET /api/users/me` → 200). Обхода авторизации в коде приложения не появилось: guard и `JwtStrategy` не тронуты

## 4. Smoke-тест инфраструктуры

- [x] 4.1 Очередь `scenario-execution` существует; у неё настроена DLQ (`RedrivePolicy` → `scenario-execution-dlq`)
- [x] 4.2 Round-trip: сообщение уходит в SQS и читается обратно; объект грузится в S3, скачивается и удаляется
- [x] 4.3 При опущенной инфраструктуре тест падает за ~30 мс с текстом «Инфраструктура не отвечает. Выполните: pnpm infra» (проверено)
- [x] 4.4 Скрипт `test:infra`; набор вынесен из обычного `pnpm test` суффиксом `.infra-spec.ts`, чтобы юнит-тесты не падали без докера

## 5. E2E на Playwright

- [x] 5.1 Подключён `@playwright/test`; браузер ставится отдельно (`npx playwright install chromium`)
- [x] 5.2 `playwright.config.ts`: проекты `setup` / `public` / `authenticated`, `storageState` с кукой сид-пользователя
- [x] 5.3 Существующий `e2e/smoke.spec.ts` наконец запускается и проходит
- [x] 5.4 Тест «создание сценария создаёт ровно один» — регрессия на дубли; за собой прибирает (DELETE сценария)
- [x] 5.5 Тест «запуск доходит до терминального статуса»: `/cards/<id>/run` → «Получить результат» → «Запустить снова», без «Ошибка выполнения» и без зависания в «Выполняем сценарий…»
- [x] 5.6 Скрипт `test:e2e` в корневом `package.json`

## 6. Проверка и документация

- [x] 6.1 Полный прогон: `pnpm infra` → `pnpm seed` → `pnpm dev` → `pnpm test:infra` (5/5) → `pnpm test:e2e` (6/6)
- [x] 6.2 Идемпотентность сида проверена тремя прогонами подряд
- [x] 6.3 Защита от прода проверена подстановкой нелокального `MONGODB_URL`
- [x] 6.4 README: сид, мок-API в таблице портов, команды `seed` / `test:infra` / `test:e2e`
- [x] 6.5 Закрыта задача 4.4 в `local-docker-infra-dev` — запуск из UI теперь покрыт e2e-тестом

## 7. Находки старого smoke-теста

- [x] 7.1 У приложения не было `<title>` вообще — старый (никогда не запускавшийся) тест это и требовал. Добавлен дефолтный заголовок в `nuxt.config.ts`
- [x] 7.2 Тест «no console errors» падал на 401 от штатной проверки сессии у анонима. Уточнён: игнорирует ожидаемый 401, ловит настоящие ошибки
