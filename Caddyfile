# Reverse proxy: /api/* -> backend (NestJS), остальное -> frontend (Nuxt).
# Backend использует глобальный префикс "api", поэтому путь не переписывается.
:8081 {
	handle /api/* {
		reverse_proxy backend:3001
	}

	handle {
		reverse_proxy frontend:5173
	}
}
